//using namespace std;
//#include<iostream>
//int myFunction()
//{
//	int numbers[5];//Static array
//	int* p;//Pointer
//	p = numbers;//P=address of first index(0) of numbers(array)
//	*p = 10;//10 is stored in the first index of numbers(array)
//	p++;//the address stored in this pointer will be incremented and now the address of 2nd index(1) will be stored in this pointer
//	*p = 20;//20 is stored in the second index of numbers(array)
//	p = & numbers[2]; //the address of 3rd index(2) will be stored in this pointer
//	*p = 30;//30 is stored in the third index of numbers(array)
//	p = numbers + 3;//the address of fourth(3) index will be stored in this pointer
//	*p = 40;//40 is stored in the 3 index of numbers(array)
//	p = numbers; // P = address of first index(0) of numbers(array)
//	*(p + 4) = 50; //50 is stored in the 4 index of numbers(array)
//	for (int n = 0; n < 5; n++)
//	cout << numbers[n] << ",";
//	return 0;
//}
//int main()
//{
//	myFunction();
//}